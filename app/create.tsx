// Web-only placeholder for desktop build. Avoid RN/Expo imports.

export default function CreateEntry() {
  return (
    <div className="p-6">
      <h2 className="text-xl font-semibold">Create</h2>
      <p className="text-muted-foreground mt-2">
        This RN/Expo screen is disabled for desktop build.
      </p>
    </div>
  )
}

// no styles needed
