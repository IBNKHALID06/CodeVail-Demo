// Web-only placeholder for desktop build. Avoid RN/Expo imports.

export default function CreateInterviewEntry() {
  return (
    <div className="p-6">
      <h2 className="text-xl font-semibold">Create Interview</h2>
      <p className="text-muted-foreground mt-2">
        This RN/Expo screen is disabled for desktop build. Use /create-interview/ route if available.
      </p>
    </div>
  )
}

// no styles needed
