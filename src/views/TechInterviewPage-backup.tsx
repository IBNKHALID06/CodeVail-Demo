// Minimal placeholder page to prevent build errors from the previous backup file.
// This route is not used in production; keeping it simple and web-only.
export default function TechInterviewPageBackup() {
  return (
    <div style={{ padding: 24 }}>
      <h1 style={{ fontSize: 24, fontWeight: 600 }}>Tech Interview (Backup)</h1>
      <p style={{ color: '#64748b', marginTop: 8 }}>
        This is a simplified backup page kept only to satisfy the Next.js pages manifest.
      </p>
    </div>
  )
}
